UPDATE_INSTRUCTIONS – Clean Rename (supabase → firebase)
Stand: 2025-08-29 07:16

1) **Alte Dateien entfernen**
   - `scripts/supabaseClient.js`
   - `scripts/supabase.js`

2) **Neue Dateien verwenden**
   - `scripts/firebaseClient.js`
   - `scripts/firebase.js`

3) **Imports**
   - Alle Module importieren ab sofort `../../scripts/firebase.js`.

4) **Firebase Config**
   - In `scripts/firebaseClient.js` die Felder `apiKey`, `messagingSenderId`, `appId` setzen.

5) **Commit & Push**
   ```bash
   git add .
   git commit -m "Clean rename to Firebase; Firestore bridge enabled"
   git push
   ```

6) **Test**
   - Verträge anlegen → Cloud‑Save → Seite reload → Cloud‑Load prüfen.
